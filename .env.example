NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
# Shared secret used to verify incoming ElevenLabs post-call webhooks.
# Generate with `openssl rand -hex 32` and paste the same value into the
# ElevenLabs agent webhook settings. Leave empty only if you are not
# exercising the webhook locally.
ELEVENLABS_WEBHOOK_SECRET=
